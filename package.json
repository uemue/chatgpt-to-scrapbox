{
  "name": "chatgpt-to-scrapbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:esbuild && npm run build:bookmarklet",
    "build:esbuild": "esbuild ./src/index.ts --bundle --outfile=dist/index.js --minify",
    "build:bookmarklet": "node ./build.js",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.1",
    "esbuild": "0.17.15",
    "jest": "^29.5.0",
    "jest-environment-jsdom": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
